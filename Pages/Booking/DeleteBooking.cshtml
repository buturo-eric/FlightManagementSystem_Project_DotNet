@page
@using System.Data.SqlClient;

<script>
    function confirmDelete() {
        return confirm("Are you sure you want to delete this Booking?");
    }
</script>

@{
    String id = Request.Query["flightID"];
    bool isDeleteConfirmed = Request.Method == "POST" && Request.Form["deleteConfirmed"] == "true";

    try
    {
        String connectionString = "Data Source=SQL5073.site4now.net;Initial Catalog=db_aa2c17_fmsdb;User Id=db_aa2c17_fmsdb_admin;Password=Hosting123!";

        if (isDeleteConfirmed)
        {
            using (SqlConnection con = new SqlConnection(connectionString))
            {
                con.Open();
                String sqlQuery = "DELETE FROM clientBookings WHERE flightID=@flightID";
                using (SqlCommand cmd = new SqlCommand(sqlQuery, con))
                {
                    cmd.Parameters.AddWithValue("@flightID", id);
                    cmd.ExecuteNonQuery();
                }
            }

            // Redirect after successful deletion
            Response.Redirect("/Booking/CheckBooking");
        }
    }
    catch (Exception ex)
    {
        Console.WriteLine("Error" + ex.Message);
    }
}

<!-- Display confirmation message and form -->
<form method="post" onsubmit="return confirmDelete()">
    <p>Are you sure you want to delete this record?</p>
    <input type="hidden" name="deleteConfirmed" value="true" />
    <button type="submit" class="btn btn-danger">Yes, Delete</button>
    <a href="/Booking/CheckBooking" class="btn btn-secondary">Cancel</a>
</form>